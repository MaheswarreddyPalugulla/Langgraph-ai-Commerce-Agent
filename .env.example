# Choose one LLM option:

# Option 1: Mock LLM (Recommended - no setup needed)
LLM_PROVIDER=mock

# Option 2: OpenAI (requires API key)
# OPENAI_API_KEY=your_openai_api_key_here
# LLM_PROVIDER=openai

# Option 3: Ollama (free but requires local setup)
# LLM_PROVIDER=ollama
# OLLAMA_MODEL=llama3.1:8b

# Test configuration - Used for order cancellation policy testing
CURRENT_TIME=2025-09-07T12:25:00Z
